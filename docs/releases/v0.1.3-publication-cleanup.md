# v0.1.3 publication cleanup

This note closes the publication loop for the current cycle.

## Included in this cleanup

- Package publish verification captured in `docs/PUBLISH-STATUS.md`
- User-facing README and quickstart are aligned with release line
- Process history doc is linked from README

## Outcome

- Release channel: active (`v0.1.3`)
- Package channel: verified (publish workflow success)
- Docs channel: user-first + operator notes separated

## Follow-up policy

- Keep package and release versions aligned
- Tag new releases only for meaningful value bundles
- For each release, append a short publication-cleanup note
