# V0.2.0-RC-CHECKLIST.md

Real-deal.

## Scope
Release-candidate checklist for BLUEPRINT Phase 0 entry (Rust workspace + core tests + NAPI v1 + TS feature-flag fallback).

## Gate status

### Gate A — Rust core tests stable
- [x] `cargo test --workspace` PASS
- [x] Deterministic hash test PASS
- [x] Chain/SOUL validation tests PASS

### Gate B — NAPI smoke stable
- [x] `memphis-napi` compiles and exports v1 contract functions
- [x] unit smoke for `chain_validate` PASS
- [x] Contract documented in `docs/NAPI-CONTRACT-V1.md`

### Gate C — TS dual-path stable
- [x] `RUST_CHAIN_ENABLED=false` default path non-breaking
- [x] Adapter test: rust flag OFF => TS legacy PASS
- [x] Adapter test: rust flag ON + missing bridge => safe fallback PASS
- [x] Full TS checks PASS (`lint`, `typecheck`, `test`, `build`)

## Quality logs (latest run)
- `cargo test --workspace` ✅
- `npm run lint` ✅
- `npm run typecheck` ✅
- `npm test` ✅
- `npm run build` ✅

## Rollback plan (if RC issues are found)
1. Disable Rust path by config:
   - `RUST_CHAIN_ENABLED=false`
2. Re-run TS quality gate
3. If deeper issue: revert recent Phase 0 commits in reverse order
4. Keep release at RC (no promotion) until gates are green again

## Promotion criteria (RC -> release)
- Two consecutive full gate runs green (A/B/C)
- No regression in existing TS endpoints/integration tests
- Team sign-off on non-breaking behavior with Rust flag default OFF
