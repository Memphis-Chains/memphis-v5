# Contributing (Memphis v4)

## Workflow
1. Small, focused changes.
2. Run checks locally: `npm run typecheck && npm run lint && npm run test`.
3. Open PR with clear scope and risks.

## Rules
- Quality over speed.
- No secrets in repo.
- No breaking API changes without versioning.
