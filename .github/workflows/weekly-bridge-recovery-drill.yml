name: weekly-bridge-recovery-drill

on:
  workflow_dispatch:

jobs:
  disabled-in-public-repo:
    runs-on: ubuntu-latest
    steps:
      - name: Safety notice
        run: |
          echo "Weekly bridge recovery drill moved to local systemd/cron for security."
          echo "Public repo self-hosted runner path is intentionally disabled."