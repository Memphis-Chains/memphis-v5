name: ollama-runtime-smoke

on:
  workflow_dispatch:

jobs:
  disabled-in-public-repo:
    runs-on: ubuntu-latest
    steps:
      - name: Safety notice
        run: |
          echo "Runtime smoke moved to local systemd timer for security."
          echo "Public repo self-hosted runner path is intentionally disabled."